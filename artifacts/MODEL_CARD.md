# Cleo 1 — Model Card

The canonical model card for the promoted Cleo 1 general-language alpha is [`../MODEL_CARD.md`](../MODEL_CARD.md).

This artifact-directory pointer intentionally avoids maintaining a second, divergent set of release metrics. The loaded web profile reads its values directly from `artifacts/cleo-1.pt` and its embedded manifests.
